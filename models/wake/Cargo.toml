[package]
name = "dn-wake-model"
version = "0.1.0"
edition = "2024"
license = "AGPL-3.0-or-later"
description = "Executable model of a two-flag (pending/might-block) Dekker-style wakeup protocol with syscall coalescing, built for schedule exploration under loom, with any preemption bound set per test."
publish = false

[target.'cfg(loom)'.dependencies]
loom = "0.7"

[lints]
workspace = true
